---
import AppLayout from "@/layouts/app-layout.astro";
import PageHeader from "@/components/common/page-header.vue";
import Container from "@/components/common/container.vue";
import Breadcrumbs from "@/components/common/breadcrumbs.vue";
import Heading1 from "@/components/common/heading-1.vue";
import Heading2 from "@/components/common/heading-2.vue";
import CalculatorPresetCarousel from "@/components/calculator-preset-carousel.astro";
import KonsolVergisi from "@/domains/konsol-vergisi/components/konsol-vergisi.vue";

import { KonsolVergisiPageDef } from "@/domains/konsol-vergisi/page-def.js";
import { getPresetsForAllBrands } from "@/domains/konsol-vergisi/db.js";
import {
    mapPresetsForCalculatorPresetCarouselComponent
} from "@/domains/konsol-vergisi/utils/map-presets-for-calculator-preset-carousel-component.js";

const konsolVergisiPage = KonsolVergisiPageDef();

const EXCHANGE_RATES = getExchangeRates();
const presets = getPresetsForAllBrands();
const presetsForCalculatorPresetCarousel = mapPresetsForCalculatorPresetCarouselComponent(presets);
---

<AppLayout page={konsolVergisiPage}>
    <PageHeader>
        <Container>
            <Breadcrumbs
                items={konsolVergisiPage.breadcrumbs}
                client:load />
            <Heading1>
                {konsolVergisiPage.title}
            </Heading1>
        </Container>
    </PageHeader>

    <section>
        <Container>
            <Heading2 tag="h2">
                Hazır hesaplamalar
            </Heading2>
        </Container>
        <CalculatorPresetCarousel
            titleTag="h3"
            items={presetsForCalculatorPresetCarousel} />
    </section>
    <hr />

    <Container>
        <div class="ta-ad-first" ta-ad-container=""></div>
    </Container>

    <section>
        <Container>
            <Heading2 tag="h2">
                Kendiniz hesaplayın
            </Heading2>
        </Container>
        <KonsolVergisi
            EXCHANGE_RATES={EXCHANGE_RATES}
            calculatorPage={konsolVergisiPage}
            client:load />
    </section>

    <Container>
        <div class="ta-ad-last" ta-ad-container=""></div>
    </Container>
</AppLayout>
